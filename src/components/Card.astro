---
---

<Fragment astro>
  {
    () => {
      const Card = ({ image, title, content }) => (
        <div class="card">
          <img src={image} alt={title} />
          <h2>{title}</h2>
          <p>{content}</p>
        </div>
      );

      return <Card />;
    }
  }
</Fragment>
